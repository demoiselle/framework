# No default secret is shipped. The application MUST configure a strong key
# (>= 32 bytes) via demoiselle.security.hashcash.hashcashKey; the module fails
# to boot otherwise.
demoiselle.security.hashcash.timetoLiveMilliseconds=5000
demoiselle.security.hashcash.difficultyBits=20
